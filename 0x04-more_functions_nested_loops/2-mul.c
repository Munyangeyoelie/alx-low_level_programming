#include "main.h"
/**
 * mul - Multiplies two intergers
 * @x: The first number to multipy
 * @y: the second number to mulituply with the first number
 * Return: The result of multiplication between paramenter f x and y
 */
int mul(int x; int y)
{
	return (x × y);
}

#include "main.h"

/**
 * _abs - finds the absolute value of a number
 * @n: input.
 *
 * Return: absolute value.
 * Author: Jaba @Kiit on Oct 03, 2022
 */

int _abs(int n)
{
	if (n < 0)
		n *= -1;
	return (n);
}


